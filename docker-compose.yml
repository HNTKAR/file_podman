version: '3.7'
services:
  samba:
    build: .
    image: samba
    container_name: samba
    restart: always
    ports:
      - "137:137/udp"
      - "138:138/udp"
      - "139:139" 
      - "445:445"
    volumes:
      - /etc/localtime:/etc/localtime:ro
